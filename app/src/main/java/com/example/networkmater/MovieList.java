package com.example.networkmater;

/*
*Gson은 JSON 문자열을 자바 객체로 변환하는데
자바는 객체를 만들 때 클래스를 먼저 정의하는 과정을 거치기 때문에
JSON 문자열을 자바 객체로 바꿀 때도 클래스를 먼저 정의해야함

 */
public class MovieList { // JSON 문자열을 자바 객체로 바꾸기 위한 클래스 정의
    MovieListResult boxOfficeResult; // 객체
}
